type Query {
    findBooks: [Book]
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
    publisher: String
}

type Author {
    id: ID
    firstName: String
    lastName: String
    age: Int
}