schema {
    query: Query
}

type Query {
    bookById(id: ID): Book
    companyById(id: ID): Company
}

type Book {
    id: ID
    name: String
    pageCount: Int
    author: Author
    publisher: String
}

type Author {
    id: ID
    firstName: String
    lastName: String
    age: Int
}